# Backend that hosts the Aomi runtime with monad-oneshot loaded.
NEXT_PUBLIC_AOMI_BACKEND_URL=http://localhost:8080

# Aomi app id — must match the slug in app/aomi.toml.
NEXT_PUBLIC_AOMI_APP_ID=monad-oneshot
